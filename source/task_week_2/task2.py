import numpy as np

import task1

# Parameter: imgArr is a numpy array with shape (n,x,y)
# Return: a numpy array with shape (n, x*y)
def multiImgToVector(imgArr):
	return np.array([[]]) # Fake return